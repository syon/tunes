extends /generate/download/_layout.jade

include /generate/download/_mixins.jade

block title
  title Silver Forest ~白銀の世界~ - mp3ファイルのダウンロード | フリー音楽素材 BGM「音の園」

block meta
  +meta('', ["SilverForest"])

block track
  .trackdetail#track_id(data-track="sf-01")
    +tracktitle("Silver Forest ~白銀の世界~", "1:34")

    p.taglist
      +taglist(["SilverForest"])

    include /generate/download/player.jade

    p
      | 

block tunescript
  script
    :coffee
      Tunes.appendSingleTrack("sf-01", "Silver Forest ~白銀の世界~")
