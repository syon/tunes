extends /generate/download/_layout.jade

include /generate/download/_mixins.jade

block title
  title バトルフィールド - mp3ファイルのダウンロード | フリー音楽素材 BGM「音の園」

block meta
  +meta('', ["RPG", "バトル", "明るい"])

block track
  .trackdetail#track_id(data-track="BattleField")
    +tracktitle("バトルフィールド", "2:49")

    p.taglist
      +taglist(["RPG", "バトル", "明るい"])

    include /app/_layouts/player.jade

    p
      | 

block tunescript
  script
    :coffee
      Tunes.appendSingleTrack("BattleField", "バトルフィールド")
